# v0.4 Policy Pack Validation Report

Status: closeout complete with sanitized productization evidence summary
Date: 2026-05-19

## Question

Can each first-class policy pack be explained, selected, and represented in
Workbench evidence without weakening the acceptance rule?

The acceptance rule remains unchanged: a run is accepted only when
`validation_report.json` has `overall_status="passed"` and
`sign_off_ready=true`, and `revision_decision.json` has
`final_status="accepted"`.

## Evidence Boundary

This report commits only sanitized summary evidence. It does not commit raw
`runs/` folders, provider logs, local absolute paths, private target-repo names,
or secrets.

Existing sanitized fixtures were reused where available. Two fresh isolated
closeout cases were run to cover the previous gaps for `low_risk_bug_fix` and
`security_privacy_sensitive`; this report records only their aggregate outcome
facts.

## Pack Cases

| Policy pack | Case type | Outcome exercised | Evidence used | Result |
|---|---|---|---|---|
| `docs_only` | Sanitized accepted run | `accept` | `examples/sample-runs/accepted-docs-only-smoke/` and `examples/pr-gate-outcomes/accepted/` | Validates the low-friction docs-only path with changed-file policy and accepted quality gate. |
| `low_risk_bug_fix` | Fresh isolated closeout run | `accept` | Ignored local run evidence summarized here | Focused pytest passed; `low_risk_bug_fix` validation passed with `sign_off_ready=true`, reason code `low_risk_bug_fix.accepted`, and quality gate `final_status="accepted"`. |
| `test_fix` | Sanitized review/block run | `block` after failed validation | `examples/sample-runs/needs-review-test-fix/` | Confirms focused test-fix evidence blocks when deterministic validation or full-suite evidence fails. |
| `api_contract_change` | Sanitized PR-gate fixture | `needs_review` | `examples/pr-gate-outcomes/needs-review/` | Confirms contract changes can require review even when deterministic contract checks pass. |
| `security_privacy_sensitive` | Fresh isolated closeout run | `review_required` after validation passed | Ignored local run evidence summarized here | Deterministic validation passed with `sign_off_ready=true`; quality gate required review with reason code `quality_loop.security_privacy_review_required`. |

## Observations

- `docs_only` is the strongest productized pack today because it has narrow
  file scope, live accepted sample evidence, and PR-gate fixture coverage.
- `api_contract_change` has strong review semantics in the PR-gate fixture and
  contract tests, but it should continue to force human compatibility review
  for meaningful public API or MCP changes.
- `low_risk_bug_fix` now has fresh accepted closeout evidence. It still depends
  on high-quality `task_test_command` evidence and exact changed-file reporting.
- `security_privacy_sensitive` is intentionally conservative. Passing public
  hygiene and the full suite is not a substitute for review when sensitive
  behavior changes; the closeout run confirmed review is required even after
  deterministic validation passes.
- The PR gate now displays `policy_pack` directly, which makes pack-specific
  outcomes easier to inspect without parsing reason codes.
- The closeout evidence used nested `tests/...` and `docs/...` paths inside the
  current recursive changed-file patterns. Top-level path ergonomics can be
  reviewed separately without changing the v0.4 acceptance rule.

## Closeout

- The two previously missing coverage points are complete as aggregate-only
  evidence.
- Raw closeout artifacts remain under ignored local `runs/` folders and are not
  committed.
- Selector output remains advisory until fresh evidence proves any automatic
  profile-selection workflow.

## Verification Commands

The implementation pass should run:

```bash
python -m pytest tests/test_policy_pack_select.py tests/test_pr_gate.py tests/test_examples.py tests/test_public_hygiene.py -q -p no:cacheprovider
python -m pytest -q -p no:cacheprovider
python tools/validate_run.py --project ai_workbench_mcp --profile scaffold --out-dir runs/v04_policy_pack_closeout_scaffold
```
