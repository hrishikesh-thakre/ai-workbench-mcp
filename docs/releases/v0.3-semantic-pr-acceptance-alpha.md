# v0.3 Semantic PR Acceptance Alpha

Release note with package upload proof.

Published Python package version: `ai-workbench-mcp==0.3.0a0`

Publication status: TestPyPI and PyPI upload plus exact-version install verification completed on 2026-05-18. MCP Registry publication completed for `0.3.0a0` on 2026-05-18.

## Scope

This alpha moves the public story from repo self-validation toward semantic PR acceptance backed by Workbench evidence.

Included repo assets:

- PR gate decision/comment behavior for `accept`, `needs_review`, and `block`.
- Scaffold-only fallback evidence blocking with `pr_gate.acceptance_evidence_missing`.
- First-class policy-pack catalog in `configs/policy_packs.yaml`.
- Five core policy packs: `docs_only`, `low_risk_bug_fix`, `test_fix`, `api_contract_change`, and `security_privacy_sensitive`.
- Copy-paste GitHub Actions template at `.github/workflows/ai-workbench-pr-gate.yml`.
- Package/bootstrap assets and `ai-workbench-bootstrap-assets` for configs, prompts, and recipes in the published `0.3.0a0` package.
- One-page external repository setup guide at `docs/github/external-repo-setup.md`.
- Separate external sample repository proof plan at `docs/github/external-sample-repo-plan.md`.
- Sanitized PR gate outcome fixtures under `examples/pr-gate-outcomes/`.

## Acceptance Rule

A PR gate can report `accept` only from real Workbench run evidence:

```text
validation_report.json
revision_decision.json
```

Green CI, a sticky PR comment, uploaded workflow artifacts, and scaffold validation are not acceptance by themselves.

## Non-Goals

- No GitHub Checks API enforcement.
- No fork-comment strategy change.
- No GEPA or extra host integrations.
- No provider plumbing or provider cost enforcement.
- No Codex cloud evidence export.

## Verification

Relevant local checks:

```bash
python -m pytest tests/test_examples.py tests/test_public_hygiene.py -q -p no:cacheprovider
```

Package release verification has completed for `ai-workbench-mcp==0.3.0a0`: build, `twine check`, fresh local wheel smoke, TestPyPI upload/install smoke, PyPI upload/install smoke, `mcp-publisher validate server.json`, MCP Registry publication, and registry lookup verification.
